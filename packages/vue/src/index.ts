/**
 * 聚合包入口：将 `@vue/runtime-dom` 再导出为单一 `vue` 模块（含响应式、`createApp`、`h` 等）。
 * 职责与并列参考目录 `vue3-main` 中的 `packages/vue` 一致，便于示例与构建脚本使用 `import { ... } from 'vue'`。
 */
export * from '@vue/runtime-dom';
