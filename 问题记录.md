- `_value` 为啥不用 `#value` 这种私有字段？

## 文档索引

各包说明文档在对应 `src` 目录下的 `*.md`：

- **reactivity**：`packages/reactivity/src/` — index、ref、reactive、effect、computed、watch、dep、baseHandlers、system
- **runtime-core**：`packages/runtime-core/src/` — index、h、renderer

## 预览命令

在项目根目录运行：

```bash
npx anywhere -p 3000
```

访问示例页：`http://localhost:3000/packages/vue/example/01-demo.html`（或 02-demo.html）。

## 备注

- 当前文档记录的是开发过程问题，不代表最终 API 设计结论。
