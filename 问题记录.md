- `_value` 为啥不用 `#value` 这种私有字段？

## 预览命令

在项目根目录运行：

```bash
npx anywhere -p 3000
```

访问模板：`http://localhost:3000/packages/**/example/**.html`

`http://localhost:3000/packages/vue/example/01-demo.html`

## 备注

- 当前文档记录的是开发过程问题，不代表最终 API 设计结论。
