- \_value为啥 不用#value这种私有变量；

## 预览命令

在 `/` 目录下运行：

```bash
npx anywhere -p 3000
```

访问地址：`http://localhost:3000/packages/**/example/**.html`

`http://localhost:3000/packages/vue/example/01-demo.html`
